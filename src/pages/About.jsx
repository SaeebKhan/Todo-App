import React from 'react';
import { tabTitle } from '../helper';

function About(props) {
    tabTitle("About");

    
    return (
        <div>
            About
        </div>
    );
}

export default About;